#!/usr/bin/env bash
# Stub pi --list-models: garbage to stdout, error to stderr, exit 1.
if [ "${1:-}" = "--list-models" ]; then
  echo "some garbage"
  echo "boom: provider registry unavailable" >&2
  exit 1
fi
exit 1
