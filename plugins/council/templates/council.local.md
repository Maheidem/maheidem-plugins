---
# Council Configuration
# This file is auto-generated by /council:setup
# Location: ~/.claude/council.local.md

initialized: true

# Enabled AI CLI tools with their configurations
enabled_tools:
  codex:
    enabled: false
    path: null
    command_template: "codex exec --skip-git-repo-check -s read-only -C \"${CWD}\" \"${PROMPT}\""
    timeout: 300
  gemini:
    enabled: false
    path: null
    command_template: "gemini -p \"${PROMPT}\" 2>&1"
    timeout: 300
  opencode:
    enabled: false
    path: null
    command_template: "opencode run --format json \"${PROMPT}\" 2>&1"
    timeout: 300
  agent:
    enabled: false
    path: null
    command_template: "agent --mode ask -p --output-format text \"${PROMPT}\" 2>&1"
    timeout: 300

# Note: PROMPT now contains structured prompts generated by invoke-cli.sh
# Additional parameters: MODE, ROUND, CONTEXT are used for thorough mode

# Default execution mode
# quick: Single round, immediate synthesis
# thorough: Multi-round debate until convergence
default_mode: quick

# Settings for thorough mode
thorough_settings:
  max_rounds: 3
  convergence_threshold: 0.8

# Display options
display:
  show_raw_responses: true
  show_timing: true
  show_agreement_analysis: true

# Enhanced prompt engineering settings
prompt_engineering:
  # Enable structured prompting for consistency
  enable_structured_prompts: true

  # Response format expectations
  response_format:
    structure: "paragraphs"  # paragraphs, bullet_points, numbered_list
    max_length: "concise"    # concise, detailed, comprehensive
    include_evidence: true   # Ask for reasoning/examples

  # Council context awareness
  council_context:
    inform_tools: true       # Tell tools they're part of a council
    specify_role: true       # Give each tool a consultation role
    encourage_diversity: true # Ask tools to bring unique perspectives

  # Quality guidelines
  quality_guidelines:
    require_reasoning: true
    encourage_examples: true
    avoid_vagueness: true
    prefer_actionable: true
---

# Council Plugin Configuration

This file stores your Council plugin preferences. Edit the YAML frontmatter above to customize behavior.

## Enabled Tools

Each tool has:
- `enabled`: Whether to include in council queries
- `path`: Full path to the CLI executable
- `command_template`: How to invoke (uses `${CWD}` and `${PROMPT}` placeholders)
- `timeout`: Maximum seconds to wait for response

## Modes

| Mode | Description |
|------|-------------|
| `quick` | Single round - all tools queried once, Claude synthesizes |
| `thorough` | Multi-round debate - tools can respond to each other |

## Safety

All tools are invoked with **READ-ONLY** permissions:
- `codex`: Uses `-s read-only` sandbox
- `gemini`: Query mode only (`-p` flag)
- `opencode`: Output-only mode (`--format json`)
- `agent`: Ask mode (`--mode ask`) - read-only Q&A

**NEVER** modify the command templates to include dangerous flags.
